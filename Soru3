List tersCevir(List liste) {
  List tersListe = [];
  for (int i = liste.length - 1; i >= 0; i--) {
    tersListe.add(liste[i]);
  }
  return tersListe;
}


void main() {
  List<String> harfler = ['a', 'b', 'c', 'd'];
  print(tersCevir(harfler)); 
}
